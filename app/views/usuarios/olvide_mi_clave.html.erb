<fieldset class="ui-widget ui-widget-content ui-corner-all">
  <legend class="ui-widget ui-widget-header ui-corner-all">Olvid&eacute; mi clave</legend>
  
  <% form_tag olvide_mi_clave_usuarios_path, :id => 'form_resetear_clave' do -%>
  
  <p><%= label_tag 'Usuario' %><br />
  <%= text_field :usuario, :login, :class =>"required ui-widget-content" %></p>

  <p><%= label_tag 'Email' %><br/>
  <%= text_field :usuario, :email, :class => 'required email ui-widget-content' %></p>



  <p>
  <%= submit_tag 'Cambiar Clave', 'class'=>'submit' %> <input type="button" class="button" value="Cancelar" onclick="location.href='/usuario/new'"/>

    <%=  image_tag('help.png', 'style'=>'vertical-align:middle', "id"=>"help", "alt"=>"Ayuda") %>
  </p>

  <% end -%>
</fieldset>
<script type="text/javascript">

$(document).ready(function(){
    $("#form_resetear_clave").validate();
     $('#help').qtip(
    {
      content: '<b>Ayuda</b><br/>Ingrese los datos solicitados en el formulario, al hacer click en <b>Cambiar Clave</b> el sistema le enviar&aacute; un correo electr&oacute;nico con los pasos a seguir.', // Give it some content
      position: 'topLeft', // Set its position
      hide: {
        fixed: true // Make it fixed so it can be hovered over
      },
      style: {
        padding: '5px 15px', // Give it some extra padding
        name: 'blue' // And style it with the preset dark theme
      }
    });
  });

</script>
